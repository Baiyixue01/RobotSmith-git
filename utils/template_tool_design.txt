Now you have a robotic task:

On the table, there is a robot arm with a parallel gripper, $3D_OBJECT_DESCRIPTION$. The task goal is to $GOAL_DESCRIPTION$. For more detailed scene setup, here is a code:

$3D_CONFIGURATION$

You need to design a tool to help finish the task.

To describe the generated tool, you must use a JSON format.
IMPORTANT: first decompose the tool construction into explicit step-by-step CAD modeling operations, then provide simulation and placement metadata.

The goal of construction_steps is geometric executability, not high-level design explanation.

The construction decomposition should follow this style:
  - Extruding : Add/Remove a [shape] at [position description(if this is not first step)], built by extruding [sketch description] by [distance].
  - Revolving: Add/Remove a [shape] at [position description(if this is not first step)], built by revolving [sketch description] around [axis/edge] by [angle].
- for every added or removed part, explicitly state:
  1) the target shape,
  2) whether it is created by extrusion or revolution,
  3) the source sketch/profile used to create it,
  4) its placement relative to the current tool.
- Use concise CAD-style wording on.
- Do not use vague appearance-oriented descriptions such as "smooth", "organic", "nice-looking", "ergonomic", "bulbous", or similar wording.
- Do not describe what the tool will do to the object inside Instruction; describe only the geometry being added or removed.


Output JSON should follow this structure (schema-style, not a literal fixed instance):
```json
{
  "name": "a string for the name of the tool",

  "construction_steps": [
    {
      "step_id": 0,
      "Instruction": "a string describing one explicit geometric modeling edit, e.g. Add a long handle, built by extruding a rectangular sketch, placed along the central axis of the cube.",
      "label": "a short abstract part label for the part introduced or modified in this step, e.g. handle",
      "is_graspable": True/False. If True, the robot arm can grasp this part; otherwise, it cannot
    },
    {
      "step_id": 1,
      "Instruction": "...",
      "label": "...",
      "is_graspable": ...
    },
    ...
  ],

  "simulation_properties": {
    "density": 1000.0,
    "friction": 0.8,
    "restitution": 0.1,
    "is_graspable_default": true
  },

  "placement_func": "def placement():\n    self.tool = scene.add_entity(\n        gs.Morph.Mesh(\n            filename=\"assembled_tool.stl\",\n            pos=(0.0, 0.0, 0.0),\n            euler=(0, 0, 0),\n            scale=(1, 1, 1),\n        )\n    )"
}
```

You should make sure:
Output valid JSON only, enclosed in ```json``` ... ```.
`construction_steps` must be complete and executable in order from 0..N, with no missing step IDs.
Each step should be atomic, explicit, and machine-readable.
In `placement_func`, provide a reasonable initial position, rotation, and scale for the designed tool in the scene.
$TIPS_FOR_DESIGNER$
The actual tool geometry will be generated later by a separate generator from construction_steps.
In placement_func, always use filename="assembled_tool.stl".